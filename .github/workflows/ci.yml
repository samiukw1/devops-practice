name: CI Pipeline

on:
  push:
    branches:
      - main
      - add-dockerfile
      - feature-readme-update

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run CI step
        run: echo "CI pipeline running..."

